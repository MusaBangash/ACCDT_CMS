# Flask Environment Configuration
# Copy this to .env and update values

# Environment: development, testing, production
FLASK_ENV=development

# Secret key for session management (generate with: python -c "import secrets; print(secrets.token_hex(32))")
SECRET_KEY=your-secret-key-here-change-in-production

# Database URL
# Development (SQLite): sqlite:///school_dev.db
# Production (PostgreSQL): postgresql://user:password@localhost/school_db
DATABASE_URL=sqlite:///school_dev.db

# Upload folder for student photos
UPLOAD_FOLDER=app/static/uploads

# Flask settings
FLASK_DEBUG=1
FLASK_APP=run.py
